{
    "name": "worlds.chat Development Container",
    "dockerFile": "Dockerfile",
    "context": "..",
    "appPort": [],
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "mhutchie.git-graph"
            ],
            "settings": {
                "terminal.integrated.shell.linux": "/bin/bash"
            }
        }
    },
    "postCreateCommand": "pip install -r /workspaces/worlds.chat/backend/requirements.txt"
}